import { useState } from "react"

function IndexPopup() {
  const [data, setData] = useState("")

  return (
    <div
      style={{
        padding: 16
      }}>
     Todo:
     Hacer un administrador de la extension

     open service worker
     open options page

     - [] options page:
      base de datos y datos de query´s y paginas visitadas

        
    </div>
  )
}

export default IndexPopup
